import { TranslateFieldPipe } from './translate-field-pipe';

describe('TranslateFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
